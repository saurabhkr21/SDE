import React from 'react'
import Child from './child'

export default function parent() {
  const array =[1,2,3].map(data=><span>{data}</span>)

  function saple(abc,...rest){
    // console.log("func",array)
    // return first+rest.join(",")
    const array=[1,2,3];
    array.push00000000000
  }
  return (
    <div>
      parent
      console.log(array)
        <Child doc="kjdahfj" render={<span>hello</span>} >
        array
        </Child>
        
    </div>
  )
}
